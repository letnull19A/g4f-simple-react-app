# Optional Proxy Configuration
# Supports HTTP, HTTPS, and SOCKS5 proxies
# Format:
#   HTTP/HTTPS: http://host:port or http://username:password@host:port
#   SOCKS5: socks5://host:port or socks5://username:password@host:port
PROXY_HTTP=
PROXY_HTTPS=

# Examples:

# HTTP Proxy (without authentication)
# PROXY_HTTP=http://proxy-server:8080
# PROXY_HTTPS=http://proxy-server:8080

# HTTP Proxy (with authentication)
# PROXY_HTTP=http://user:pass@proxy-server:8080
# PROXY_HTTPS=http://user:pass@proxy-server:8080

# SOCKS5 Proxy (without authentication)
# PROXY_HTTP=socks5://proxy-server:1080
# PROXY_HTTPS=socks5://proxy-server:1080

# SOCKS5 Proxy (with authentication)
# PROXY_HTTP=socks5://user:pass@proxy-server:1080
# PROXY_HTTPS=socks5://user:pass@proxy-server:1080
